import { HeightPipePipe } from './height-pipe.pipe';

describe('HeightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
